{
  "name": "@startier/ohrid-jsonrpc-driver",
  "version": "1.2.0",
  "description": "A driver for @startier/ohrid that uses JSON-RPC 2.0 and socket.io for remote calls",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "keywords": [],
  "author": "Mihail Mojsoski",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "@types/node": "^20.9.2",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "watch": "npx tsc --watch",
    "build": "npx tsc",
    "prepare": "npm run build",
    "version": "git add -A lib",
    "postversion": "git push && git push --tags"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@mojsoski/rpc": "^1.0.0",
    "@startier/ohrid": "^1.0.1",
    "socket.io": "^4.7.2",
    "socket.io-client": "^4.7.2"
  }
}
